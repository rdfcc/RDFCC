---
// library imports





// component imports
import Link from "./Link.astro";
import ImageCard from "../components/ImageCard.astro"

// utils imports
import { slugify, formatDate } from "../scripts/utils";

const { frontmatter, url, tagType } = Astro.props;

const Tag = tagType;

const { title, pubDate, author, image, description } = frontmatter;


---

<article class="group  justify-end items-end bg-gray-100 overflow-hidden rounded-xl shadow-lg relative hover:shadow-xl">
  
  <a href={url} aria-hidden="true" tabindex="-1">
    <div class="flex justify-center  w-full">
    <ImageCard 
      imagePath={image.url}
      altText={image.alt};
      />
    </div>
  </a>
  <div class="content">
    <div>
      <Tag class="h3">
        <a href={url} class="font-bold">{title}</a>
      </Tag>
      <small>
        by {author}
          {formatDate(pubDate)}
        
      </small>
    </div>
    <p>{description}</p>
    <Link href={url} text="Read Post" style="secondary" />
  </div>
</article>